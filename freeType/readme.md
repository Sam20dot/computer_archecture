# free type is library which help us to take the text and turn them into the glyph on the scrren 

* then for now i am reading about the header section and what to include 
1. in compilation we include
    pkg-config --cflag freetype2

2. header file one is 
      ft2build.h
      and then we include the header which is FT_FREETYPE_H
      
