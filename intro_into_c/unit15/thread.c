// thread is what allow us to do parallel processing of the infromations 
// but they can bring the overhead for given so sometime when they are not doing well can introduce the overhead 
#include <stdio.h>
#include <stdlib.h>
#include <threads.h>



int main () {


    // we are going to measure the size of thread 
    pthread_t thrd;

    printf ("the thread size if the following :%zu",sizeof (thrd));





}
