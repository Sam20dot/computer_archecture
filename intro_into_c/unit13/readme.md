# INTEGER WITH EXACT N -BIT 

to mean that this is the interger with the size which is constant no matter what is the arrchecture of the machine 
or the type of the device 
we have what we call the general form of the interger which can be used to sepcify that 
## the form or syntax is :
    
##    intN_t 
    that N is the number of the bit we want to use it can be 8,16,32,64,128 
    in example they can look like this 
    EXAMPLE : int8_t int16_t int32_t or even int64_t  


## we can use the marcos to show or to inter them which are found in the header which has been included in 
## standards which is called;

<inttype.h> this header is used for including those all marcos which will be needed 

every macros is started with for printf 
 PRI 
 ## example     
       PRIdN
       PRIiN
       PRIoN
       PRIxN
       PRIxN
for scan f we use the following it gonna be like for printf but we use 
    SCN.

 ## example :
      SCNdN
      SCNiN
      SCNoN
      SCNxN
      
  
