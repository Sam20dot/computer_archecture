# CONDITIONAL COMPILATIONS 

those are statements which are used during compilations 
they are used to tell the compiler if a given identfier has been declared to compiler but if not provided 
it will not compile that part of the programs we have them but what can be used for long time is 


## #ifdef :
     the codes which are in this section or statements will be compilered if somethings has been defined 
     when defined that part is going to be compilered and ended up with the 

## #endif 

## syntax :
    #ifdef identifier
    //statements 
    // to be compiled 

    #endif 

when that part is not defined we are going to go on else part which is the second options 
## #ifndef :
    when the defined values was not given we will compile the codes in this sections also will end with 
    the following 
## #endif 

## syntax :
     
      #ifndef identifier 
      // statements 
      //to be compiled 


### the next will be the examples which will be displayed in the forder which will be attached within this file thank you for what makes me strong guy

