# in assembly we have two ways of control here we have to use the following two instruction 

1. test : it uses and instsead of using the sub 
2. cmp  : this instruction uses sub to mean that it takes the number which is in one register 
        and sub with the other in other register but it does not store anythings what it do 
        it set the conditional flag based on the answers which determines the flow of the codes 
        we have different flags
## flags :
        zero flag : this occurs when we are the comparizon returns zero values 
        sign flag : this becomes one when the result is negative balues or positive values 
        carry flag :this becomes one when the the results values on signed values is larger than the actual size of the part 
        of the registers 
        overflaw flag : this also is the same as carry flag but they are deffer from the different one works on the signed and other works 
        on unsigned vaues 


